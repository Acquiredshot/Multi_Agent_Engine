# Per-queue / per-object metrics for the Prometheus plugin. Off by default in
# RabbitMQ 3.13+: without it the /metrics endpoint only exposes queue metrics
# aggregated over all queues (no `queue` label), which the dashboard needs.
prometheus.return_per_object_metrics = true