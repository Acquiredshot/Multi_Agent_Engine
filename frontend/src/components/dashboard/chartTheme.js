/** Shared recharts axis/grid props so every chart feels identical. */
export const AXIS_PROPS = {
  stroke: "#3f4754",
  tick: { fill: "#6b7482", fontSize: 10, fontFamily: "JetBrains Mono, monospace" },
  tickLine: false,
  axisLine: { stroke: "#1c222c" },
}

export const GRID_PROPS = {
  stroke: "#161b23",
  strokeDasharray: "3 3",
  vertical: true,
  horizontal: true,
}
